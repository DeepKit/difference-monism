# ECET History

## 2026-03-30 v1.1 First-Round Structural Convergence

本轮完成了当时活动任务台所要求的首轮主链收束与工具层补建，当前结果如下：

- 为 `A00 / A01 / B03 / B07 / B09 / C13 / C11` 补入统一的 `DM / ASTO` 继承合同。
- 将 `A01` 压回真正入口件，固定最小主链 / 主链伴随件 / 扩展层。
- 将 `B09 <-> C17` 证据链修复为 `H1a / H2a / H4a` 子假设接口。
- 在 `B03 / C13 / A01` 明确“结构判断 -> 规范桥梁 -> 程序条件”的阅读纪律。
- 将 `B05` 的形式化补充拆出为 `B05a`，将 `B07 / C13 / C11` 的尾部补充节并回主结构。
- 新建 `P99.1-4`，开始锁术语、失败条件、证据矩阵与版本修订。
- 统一 `readme / PUBLISH_READINESS / ECET.预印本发布说明.v1.0` 的主链口径。
- 将 `C15` 明确为来源说明稿，不再与最小主链竞争。

## 当前口径

- `ECET` 适合公开征评与预印本式发布。
- `ECET` 尚未完成规模化经验闭合。
- `C17` 当前只支撑子假设层，不代表正式主假设已被全量验证。
- 工具层已经建立，但仍以“辅助对齐与答辩”为主，不应压过主链正文。

## 2026-03-31 v1.2 Historical-Comparison Interface Consolidation

本轮完成了 `ECET` 第二轮公共化整改中最关键的一步：把历史材料、文明比较与正式假设之间的接口真正写回主链。

- 在 `README.md` 新增“历史个案进入主链的最小接口”，明确 `P99.5 / P99.6` 与 `B09` 的固定关系。
- 在 `ECET.B09_可检验假设.md` 新增“历史与跨文明材料如何进入 B09”，把 `H3 / H6 / H7 / H10` 的历史接口写成正式纪律。
- 进一步写死：单一个案通常最多只能提供 `E1 / E2` 级支持；没有跨案例对照与替代解释时，不得直接上升为 `E3 / E4`。
- 当前 `ECET` 的公开主位因此更清楚地转为：`文明比较框架 + 假设接口 + 可比性纪律`，而不是“会解释一切的文明总论”。

## 2026-03-31 v1.3 活动任务台迁入 History

- 已将当时活动任务台中关于本轮完成态的摘要迁入 `history.md`。
- 当时的活动任务台随后只保留后续写作与补证时仍有效的活动纪律。

## 活动任务台清空收口

- 已继续检查当时活动任务台中的剩余三项，确认它们都属于后续写作纪律，而不是当前未完成阻塞项。
- 后续若新增文明个案稿，固定直接套用 `P99.5` 壳体。
- 后续若新增公开比较稿，固定先按 `P99.6` 标级，再决定是否进入主文。
- 后续若新增仿真或经验材料，固定先回接 `B09`，不得先下文明裁决。
- 当时的活动任务台已因此压到“当前无活动任务”状态。
- 当时该层目录随后不再单独保留活动任务台；后续若再扩写，统一先记入 `history.md` 或对应正式母文件。
